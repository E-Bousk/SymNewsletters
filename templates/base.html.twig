<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <a href="{{ path('newsletters_home') }}">S'abonner aux newsletters</a>
        <br><a href="{{ path('newsletters_prepare') }}">Preparer une newsletter</a>
        <br><a href="{{ path('newsletters_list') }}">Lister/envoyer les newsletters</a>
        <br><a href="{{ path('newsletters_failed') }}">Gérerer les échecs d'envoi</a>
        {% block body %}{% endblock %}
    </body>
</html>
